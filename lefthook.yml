pre-commit:
  commands:
    biome-check:
      glob: "*.{js,ts,jsx,tsx,json,css,astro,svelte}"
      run: pnpm biome check --no-errors-on-unmatched --files-ignore-unknown=true {staged_files}

commit-msg:
  commands:
    commitlint:
      run: pnpm commitlint --edit {1}
